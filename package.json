{
  "name": "script-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:manifest": "node scripts/build-manifest.js",
    "deploy": "npm run build:manifest && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "marked": "^12.0.0",
    "wrangler": "^3.0.0"
  }
}
