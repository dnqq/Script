{
  "name": "script-gallery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./public/styles.css -o ./public/output.css",
    "deploy": "npm run build && wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "wrangler": "^3.0.0"
  }
}
